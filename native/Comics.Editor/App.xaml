<Application x:Class="Comics.Editor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:Comics.Editor"
			 xmlns:vm="clr-namespace:Comics.Editor.ViewModel"
			 Startup="Application_Startup"
             StartupUri="Windows/MainWindow.xaml">
	<Application.Resources>
		<sys:Double x:Key="ratio">1.4</sys:Double>
		<sys:Int32 x:Key="upOffset">-1</sys:Int32>
		<sys:Int32 x:Key="downOffset">1</sys:Int32>
		<sys:Double x:Key="thumbSize">10</sys:Double>
		<sys:Double x:Key="soundSize">16</sys:Double>
		<vm:RatioConverter x:Key="ratioConverter" />
		<vm:ImagePathConverter x:Key="imagePathConverter" />
		<vm:EqualsConverter x:Key="equalsConverter" />
		<vm:EnumConverter x:Key="enumConverter" />
		<vm:ScrollConverter x:Key="startScrollConverter" />
		<vm:ScrollConverter x:Key="endScrollConverter" />
		<vm:ThumbConverter x:Key="thumbConverter" />
		<vm:PivotConverter x:Key="pivotConverter" />
		<vm:SoundConverter x:Key="soundConverter" />
		<Style x:Key="layerButton" TargetType="{x:Type ToggleButton}">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type ToggleButton}">
						<Grid Background="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}">
							<ContentPresenter />
							<Rectangle Name="rect" Visibility="Collapsed">
								<Rectangle.Fill>
									<SolidColorBrush Color="#7FBFFF" Opacity="0.4" />
								</Rectangle.Fill>
							</Rectangle>
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger Property="IsChecked" Value="True">
								<Setter TargetName="rect" Property="Visibility" Value="Visible" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style x:Key="soundButton" TargetType="{x:Type ToggleButton}">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type ToggleButton}">
						<Grid Background="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}">
							<ContentPresenter />
						</Grid>
						<ControlTemplate.Triggers>
							<Trigger Property="IsChecked" Value="True">
								<Setter Property="Background" Value="#CBE5FF" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Application.Resources>
</Application>
