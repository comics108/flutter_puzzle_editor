# Run with `dart run ffigen --config ffigen.yaml`.
name: EditorBindings
description: |
  Bindings for `src/editor.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/editor_bindings_generated.dart'
headers:
  entry-points:
    - 'src/editor.h'
  include-directives:
    - 'src/editor.h'
comments:
  style: any
  length: full
